// COMMENT: index.js in this dir is the central hub that imports all of the models and packages them into an object that can be exported and used by the rest of the application
import User from "./User.js";
import Thought from "./Thought.js";

export { User, Thought };
